'use client'
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {ArrowLeft,Bell} from 'lucide-react'
export default function Page(){const router=useRouter();const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/customer/notifications').then(r=>r.json()).then(x=>setRows(x.data?.notifications||[]))},[]);return <main className="min-h-screen bg-background"><div className="mx-auto max-w-2xl p-4 md:p-8"><button onClick={()=>router.push('/customer')} className="inline-flex items-center gap-2 rounded-xl border px-4 py-2 text-xs"><ArrowLeft className="size-4"/>Marketplace</button><h1 className="mt-6 text-2xl font-bold">Notifikasi</h1><div className="mt-5 divide-y rounded-2xl border bg-card">{rows.length?rows.map((r,i)=><div key={i} className="flex gap-3 p-4"><Bell className="mt-1 size-4 text-primary"/><div><p className="text-sm font-semibold">{r.title}</p><p className="mt-1 text-xs text-muted-foreground">{r.description}</p></div></div>):<p className="p-10 text-center text-xs text-muted-foreground">Belum ada notifikasi.</p>}</div></div></main>}
