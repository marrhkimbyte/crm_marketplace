'use client'
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {ClipboardList,ArrowLeft} from 'lucide-react'
const money=(v:number)=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(v)
export default function Page(){const router=useRouter();const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/customer/orders',{cache:'no-store'}).then(r=>r.json()).then(x=>setRows(x.data?.orders||[]))},[]);return <main className="min-h-screen bg-background"><div className="mx-auto max-w-5xl p-4 md:p-8"><button onClick={()=>router.push('/customer')} className="inline-flex items-center gap-2 rounded-xl border px-4 py-2 text-xs"><ArrowLeft className="size-4"/>Marketplace</button><h1 className="mt-6 text-2xl font-bold">Pesanan Saya</h1>{!rows.length?<div className="mt-6 rounded-2xl border bg-card p-12 text-center"><ClipboardList className="mx-auto size-10 text-muted-foreground"/><p className="mt-3 font-semibold">Belum ada pesanan</p></div>:<div className="mt-6 space-y-3">{rows.map(r=><div key={r.id} className="flex flex-col gap-3 rounded-2xl border bg-card p-5 md:flex-row md:items-center"><div className="flex-1"><p className="text-xs text-muted-foreground">#{r.id} · {new Date(r.created_at).toLocaleString('id-ID')}</p><p className="mt-1 font-semibold">{r.product_name}</p><p className="mt-1 text-xs text-muted-foreground">{r.quantity} unit</p></div><b>{money(Number(r.total_price))}</b><span className="rounded-full bg-primary/10 px-3 py-1 text-[10px] font-semibold text-primary">{r.status}</span></div>)}</div>}</div></main>}
