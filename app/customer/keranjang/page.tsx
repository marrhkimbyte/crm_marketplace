'use client'
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {Minus,Plus,Trash2,ArrowRight,ShoppingBag} from 'lucide-react'
type I={id:number;name:string;price:number;stock:number;image:string|null;quantity:number};const money=(v:number)=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(v)
export default function Page(){const router=useRouter();const [items,setItems]=useState<I[]>([]);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem('crm_cart')||'[]'))}catch{}},[]);useEffect(()=>localStorage.setItem('crm_cart',JSON.stringify(items)),[items]);const total=items.reduce((s,i)=>s+i.price*i.quantity,0);const setQ=(id:number,q:number)=>setItems(x=>x.map(i=>i.id===id?{...i,quantity:Math.max(1,Math.min(i.stock,q))}:i));return <main className="min-h-screen bg-background"><div className="mx-auto max-w-5xl p-4 md:p-8"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-bold">Keranjang</h1><p className="text-xs text-muted-foreground">Produk yang siap kamu pesan.</p></div><button onClick={()=>router.push('/customer')} className="rounded-xl border px-4 py-2 text-xs">Lanjut Belanja</button></div>{!items.length?<div className="mt-8 rounded-2xl border bg-card p-12 text-center"><ShoppingBag className="mx-auto size-10 text-muted-foreground"/><p className="mt-3 font-semibold">Keranjang masih kosong</p></div>:<div className="mt-6 grid gap-5 lg:grid-cols-[1fr_320px]"> <div className="space-y-3">{items.map(i=><div key={i.id} className="flex gap-4 rounded-2xl border bg-card p-4">{i.image?<img src={i.image} className="size-24 rounded-xl object-cover"/>:<div className="size-24 rounded-xl bg-muted"/>}<div className="min-w-0 flex-1"><h3 className="font-semibold">{i.name}</h3><p className="mt-1 text-sm font-bold text-primary">{money(i.price)}</p><div className="mt-3 flex items-center gap-2"><button onClick={()=>setQ(i.id,i.quantity-1)} className="size-8 rounded-lg border"><Minus className="mx-auto size-3"/></button><span className="text-xs">{i.quantity}</span><button onClick={()=>setQ(i.id,i.quantity+1)} className="size-8 rounded-lg border"><Plus className="mx-auto size-3"/></button></div></div><button onClick={()=>setItems(x=>x.filter(a=>a.id!==i.id))} className="text-destructive"><Trash2 className="size-4"/></button></div>)}</div><div className="h-fit rounded-2xl border bg-card p-5"><p className="text-xs text-muted-foreground">Total</p><p className="mt-2 text-2xl font-bold">{money(total)}</p><button onClick={()=>router.push('/customer/checkout')} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-xs font-semibold text-primary-foreground">Checkout <ArrowRight className="size-4"/></button></div></div>}</div></main>}
