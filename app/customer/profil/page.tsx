'use client'
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {ArrowLeft,User} from 'lucide-react'
export default function Page(){const router=useRouter();const [u,setU]=useState<any>(null);const [phone,setPhone]=useState('');const [address,setAddress]=useState('');const [msg,setMsg]=useState('');useEffect(()=>{fetch('/api/customer/profile').then(r=>r.json()).then(x=>{setU(x.data?.user);setPhone(x.data?.user?.phone||'');setAddress(x.data?.user?.address||'')})},[]);async function save(){const r=await fetch('/api/customer/profile',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,address})});const x=await r.json();setMsg(x.success?'Profil berhasil diperbarui.':x.message||'Gagal menyimpan.')}return <main className="min-h-screen bg-background"><div className="mx-auto max-w-2xl p-4 md:p-8"><button onClick={()=>router.push('/customer')} className="inline-flex items-center gap-2 rounded-xl border px-4 py-2 text-xs"><ArrowLeft className="size-4"/>Marketplace</button><div className="mt-6 rounded-3xl border bg-card p-6"><div className="flex items-center gap-3"><div className="flex size-12 items-center justify-center rounded-xl bg-primary/10 text-primary"><User/></div><div><h1 className="text-xl font-bold">Profil Saya</h1><p className="text-xs text-muted-foreground">{u?.email}</p></div></div><label className="mt-6 block text-xs font-medium">Nomor Telepon<input value={phone} onChange={e=>setPhone(e.target.value)} className="mt-2 h-11 w-full rounded-xl border px-3 text-sm"/></label><label className="mt-4 block text-xs font-medium">Alamat<textarea value={address} onChange={e=>setAddress(e.target.value)} rows={4} className="mt-2 w-full rounded-xl border p-3 text-sm"/></label><button onClick={save} className="mt-5 w-full rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">Simpan Profil</button>{msg&&<p className="mt-3 text-xs text-emerald-600">{msg}</p>}</div></div></main>}
